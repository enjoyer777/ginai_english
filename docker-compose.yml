services:
  bot:
    build: .
    image: school-english-pro-bot:latest
    container_name: school-english-pro-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
